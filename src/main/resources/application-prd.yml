server:
  port: 81

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gsxz?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: admin

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml


DIR_MAX_NUM: 1000
AVATAR_FILE_PATH: /gxsz/avatar/
HEAD_FILE_PATH: /data/www/data